using System.Windows.Forms;

namespace Biblioteca.Controles
{
    public partial class MiGridSelection : DataGridView
    {
        public MiGridSelection()
        {
            InitializeComponent();
        }

        protected override void OnPaint(PaintEventArgs pe)
        {
            base.OnPaint(pe);
        }
    }
}
