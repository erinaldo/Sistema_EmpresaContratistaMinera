using System.Windows.Forms;

namespace Biblioteca.Controles
{
    public partial class MiGridEdit : DataGridView
    {
        public MiGridEdit()
        {
            InitializeComponent();
        }

        protected override void OnPaint(PaintEventArgs pe)
        {
            base.OnPaint(pe);
        }
    }
}
